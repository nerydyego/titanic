[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "titanic"
version = "0.1.0"
description = "Projeto de Machine Learning para prever sobrevivência no Titanic"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]